# K-Evolution 네이밍 규칙

이 문서는 현재 코드베이스에 **실제로 적용되어 있는** 네이밍 규칙을 정리한 것입니다.
새로 코드를 추가할 때는 아래 규칙을 따라 일관성을 유지합니다.

---

## 1. 패키지 구조

```
com.kevolution
 ├─ controller   // 요청 처리 (@Controller)
 ├─ service      // 비즈니스 로직 (@Service)
 ├─ repository   // 데이터 접근 (JpaRepository)
 ├─ entity       // JPA 엔티티
 └─ config       // 설정 (Security, AES 등)
```

---

## 2. 엔티티 (Entity)

| 대상 | 규칙 | 예시 |
|---|---|---|
| 클래스명 | PascalCase, **단수** | `Product`, `OrderItem`, `MemberAddress` |
| 테이블명 `@Table(name=)` | snake_case, **단수** | `product`, `order_item`, `member_address` |
| 필드명 | camelCase (DB는 자동 snake_case 변환) | `createdAt` → `created_at` |
| PK 필드 | `엔티티명 + Id` | `productId`, `orderId` |
| FK 연관 필드 | 참조 대상명 (camelCase) | `member`, `product`, `category` |
| FK 컬럼 `@JoinColumn` | `참조대상_id` | `member_id`, `product_id` |

### PK 전략
- **기본**: `Long` + `@GeneratedValue(strategy = GenerationType.IDENTITY)` → `BIGINT AUTO_INCREMENT`
- **회원(`Member`)만 예외**: `String` UUID + `@UuidGenerator` → `CHAR(36)`
  (비순차·비추측 외부 식별자. 회원을 참조하는 FK 컬럼은 모두 `CHAR(36)`)

### boolean 필드
- 필드명은 `is`를 **뺀다**, 컬럼은 `@Column(name = "is_xxx")`로 명시
- 예: 필드 `used` → 컬럼 `is_used`, 필드 `active` → 컬럼 `is_active`,
  필드 `pinned` → 컬럼 `is_pinned`, 필드 `secret` → 컬럼 `is_secret`

```java
@Column(name = "is_used", nullable = false)
private boolean used;
```

### enum
- 엔티티 내부에 **중첩 enum**으로 선언, `@Enumerated(EnumType.STRING)` 매핑
- 예: `Member.Role { USER, ADMIN }`, `Order.Status { PENDING, PAID, CANCELLED }`

### 시간 필드
- `createdAt`(등록), `updatedAt`(수정) — `LocalDateTime`
- `@PrePersist`에서 생성 시각, `@PreUpdate`에서 수정 시각 세팅
- `createdAt`은 `updatable = false`

### 공통 어노테이션 패턴
```java
@Entity
@Table(name = "xxx")
@Getter
@NoArgsConstructor(access = AccessLevel.PROTECTED)
public class Xxx {
    // ...
    @Builder
    public Xxx(...) { ... }   // 생성은 빌더로
}
```
- 연관관계는 기본 `@ManyToOne(fetch = FetchType.LAZY)`
- 개인정보(전화번호·주소·CI 등)는 `@Convert(converter = AesAttributeConverter.class)`로 AES-256 암호화 저장

---

## 3. 리포지토리 (Repository)

| 대상 | 규칙 | 예시 |
|---|---|---|
| 인터페이스명 | `엔티티명 + Repository` | `ProductRepository`, `ReviewRepository` |
| 상속 | `JpaRepository<엔티티, PK타입>` | `JpaRepository<Product, Long>`, `JpaRepository<Member, String>` |

- 단순 조회는 **Spring Data 파생 쿼리** 메서드명 규칙을 따른다
  - `findByXxx`, `existsByXxx`, `countByXxx`, `findByXxxOrderByYyyDesc`
  - 예: `findByUserId`, `existsByUserId`, `findByProductOrderByCreatedAtDesc`, `countByProduct`
- 복잡한 쿼리는 `@Query`(JPQL) 사용
  - 예: `findPopularProducts` (LEFT JOIN + GROUP BY + ORDER BY)

---

## 4. 서비스 (Service)

| 대상 | 규칙 | 예시 |
|---|---|---|
| 클래스명 | `도메인명 + Service` | `ProductService`, `MemberService` |
| 의존성 주입 | `final` 필드 + `@RequiredArgsConstructor` (생성자 주입) | |

```java
@Service
@RequiredArgsConstructor
@Transactional(readOnly = true)   // 클래스 기본은 읽기 전용
public class ProductService {
    @Transactional                // 쓰기 메서드만 재정의
    public Long createProduct(...) { ... }
}
```

- 메서드명은 동사로 시작: `getProducts`, `getProduct`, `createProduct`, `updateProduct`, `deleteProduct`

---

## 5. 컨트롤러 (Controller) · URL

| 대상 | 규칙 | 예시 |
|---|---|---|
| 클래스명 | `도메인명 + Controller` | `ProductController`, `NoticeController` |
| URL 리소스 | **소문자 · 복수형** | `/products`, `/orders`, `/admin/products` |
| 인증 | `/auth/...` | `/auth/login`, `/auth/signup`, `/auth/logout` |
| 관리자 | `/admin/...` (ROLE_ADMIN 전용) | `/admin/products` |

> **권한별로 클래스를 쪼개지 않는다.** 한 도메인은 컨트롤러·서비스 하나로 두고,
> 관리자 기능은 클래스 이름(`Admin*`)이 아니라 `/admin/**` URL(SecurityConfig)로만 구분한다.
> 예) 공지 조회·관리는 `NoticeController` 한 곳, 상품 조회·관리는 `ProductController` 한 곳.

### CRUD URL · 핸들러 메서드 규칙

> **원칙: URL의 마지막 동사 = 메서드명으로 통일한다.**
> 폼(GET)과 처리(POST)는 **같은 URL**을 쓰고, GET 폼 메서드만 `Form` 접미사를 붙인다.

| 기능 | HTTP · URL | 메서드명 |
|---|---|---|
| 목록 | `GET /products` | `list` |
| 상세 | `GET /products/{id}` | `detail` |
| 등록 폼 | `GET /products/register` | `registerForm` |
| 등록 처리 | `POST /products/register` | `register` |
| 수정 폼 | `GET /products/{id}/edit` | `editForm` |
| 수정 처리 | `POST /products/{id}/edit` | `edit` |
| 삭제 처리 | `POST /products/{id}/delete` | `delete` |

- 등록은 `register` URL로 GET·POST 한 쌍, 수정은 `/{id}/edit` URL로 GET·POST 한 쌍
  → URL만 봐도 어떤 메서드인지 바로 안다 (`register`/`registerForm`, `edit`/`editForm`)
- 등록 폼은 `/register`(REST식 `/new` 대신 한글 "등록"과 일치)
- 조회는 GET, 상태 변경(등록·수정·삭제)은 **POST** (삭제도 GET 아님)

---

## 6. 뷰 (JSP)

| 대상 | 규칙 | 예시 |
|---|---|---|
| 경로 | `/WEB-INF/views/{리소스}/{화면}.jsp` | `products/list.jsp`, `admin/products/form.jsp` |
| 파일명 | 소문자 | `list`, `detail`, `form`, `register`, `login` |
| 공통 조각 | `fragments/` | `fragments/header.jsp` |

- 등록·수정 화면은 가능하면 **하나의 `form.jsp`**로 공용화하고, 모델에 수정 대상 객체 유무로 분기
- taglib 접두사: `c`(JSTL core), `fmt`(포맷), `sec`(Spring Security)
- 모든 POST 폼에는 CSRF 토큰 포함:
  ```jsp
  <input type="hidden" name="${_csrf.parameterName}" value="${_csrf.token}"/>
  ```

---

## 7. DB 스키마 (MySQL)

| 대상 | 규칙 | 예시 |
|---|---|---|
| 테이블명 | snake_case, **단수** | `member`, `product`, `order_item` |
| 예외 | SQL 예약어는 복수형 회피 | `orders` (`order`가 예약어) |
| PK 컬럼 | `테이블명_id` | `product_id`, `member_id` |
| FK 컬럼 | `참조테이블_id` | `category_id`, `order_id` |
| boolean 컬럼 | `is_xxx` | `is_used`, `is_active` |
| 일반 컬럼 | snake_case | `created_at`, `final_price` |

### 제약·인덱스 접두사
| 종류 | 접두사 | 예시 |
|---|---|---|
| 인덱스 | `idx_테이블_컬럼` | `idx_product_category` |
| FK 제약 | `fk_테이블_참조` | `fk_order_item_product` |
| UNIQUE | `uk_테이블_컬럼` | `uk_member_user_id` |

- 공통: `ENGINE=InnoDB`, `CHARSET=utf8mb4`, `COLLATE=utf8mb4_unicode_ci`
- 모든 컬럼에 `COMMENT`로 한글 설명 부여
- 스키마 원본은 `db/schema.sql` (자식 → 부모 역순 DROP 후 CREATE)

---

## 8. CSS

- 파일: `src/main/resources/static/css/main.css`
- 클래스명: **kebab-case** — `category-chip`, `product-card`, `section-title`, `rank-badge`, `hero-search`

---

## 9. 요약 체크리스트

새 도메인을 추가할 때:

- [ ] 엔티티: PascalCase 단수, 테이블 snake_case 단수, PK `Long xxxId`(회원 참조는 `CHAR(36)`)
- [ ] boolean은 `is_xxx` 컬럼 + `is` 뺀 필드명
- [ ] Repository: `XxxRepository extends JpaRepository<Xxx, IdType>`
- [ ] Service: `@Transactional(readOnly=true)` 기본 + 쓰기 메서드 `@Transactional`
- [ ] Controller: 소문자 복수 URL, CRUD는 list/detail/registerForm/register/editForm/edit/delete (URL 동사 = 메서드명)
- [ ] JSP: `/WEB-INF/views/{리소스}/{화면}.jsp`, POST 폼엔 CSRF 토큰
- [ ] DB: `idx_` / `fk_` / `uk_` 접두사, utf8mb4, 컬럼 COMMENT
