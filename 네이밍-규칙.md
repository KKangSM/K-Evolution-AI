# K-Evolution 네이밍 규칙

이 문서는 현재 코드베이스에 **실제로 적용되어 있는** 네이밍 규칙을 정리한 것입니다.
새로 코드를 추가할 때는 아래 규칙을 따라 일관성을 유지합니다.

---

## 1. 패키지 구조 (도메인 우선 · 도메인/레이어)

최상위는 **도메인**으로 나누고, 그 안을 **레이어**(controller/service/repository/entity)로 둔다.
레이어를 최상위로 두지 않는다.

```
com.kevolution
 ├─ {도메인}
 │   ├─ controller   // 요청 처리 (@Controller)
 │   ├─ service      // 비즈니스 로직 (@Service)
 │   ├─ repository   // 데이터 접근 (JpaRepository)
 │   └─ entity       // JPA 엔티티
 │   (도메인이 필요한 레이어만 둔다. 예: cart 는 entity·repository 만 존재)
 │
 ├─ 도메인 예시: auth, member, product, banner, notice, qna, terms,
 │              support, mypage, cart, order, coupon, review,
 │              wishlist, pointhistory, returnrequest ...
 │
 ├─ config         // 횡단 설정 (SecurityConfig, WebConfig, AES, 정책, GlobalModelAdvice)
 ├─ storage        // 횡단 인프라 (SupabaseStorageService — 이미지 업로드)
 └─ controller     // 도메인에 안 속하는 공통 화면 (MainController, DashboardController, WellKnownController)
```

- 한 도메인의 코드는 그 도메인 패키지 안에 모은다. (예: 배너 관련은 전부 `com.kevolution.banner.*`)
- 여러 도메인이 함께 쓰는 횡단 관심사만 `config` / `storage` / 최상위 `controller` 에 둔다.

---

## 2. 엔티티 (Entity)

| 대상 | 규칙 | 예시 |
|---|---|---|
| 클래스명 | PascalCase, **단수** | `Product`, `OrderItem`, `Banner` |
| 테이블명 `@Table(name=)` | snake_case, **단수** | `product`, `order_item`, `banner` |
| 필드명 | camelCase (DB는 snake_case 변환) | `createdAt` → `created_at` |
| PK 필드 | `엔티티명 + Id` | `productId`, `orderId`, `bannerId` |
| FK 연관 필드 | 참조 대상명 (camelCase) | `member`, `product`, `category` |
| FK 컬럼 `@JoinColumn` | `참조대상_id` | `member_id`, `product_id`, `category_id` |

### PK 전략
- **기본**: `Long` + `@GeneratedValue(strategy = GenerationType.IDENTITY)` → `BIGINT AUTO_INCREMENT`
- **회원(`Member`)만 예외**: `String` UUID + `@UuidGenerator` → `CHAR(36)`
  (비순차·비추측 외부 식별자. 회원을 참조하는 FK 컬럼은 모두 `CHAR(36)`)

### boolean 필드
- 필드명은 `is`를 **뺀다**, 컬럼은 `@Column(name = "is_xxx")`로 명시
- 예: 필드 `active` → 컬럼 `is_active`, 필드 `pinned` → 컬럼 `is_pinned`,
  필드 `used` → 컬럼 `is_used`, 필드 `secret` → 컬럼 `is_secret`

```java
@Column(name = "is_active", nullable = false)
private boolean active;
```

### enum
- 엔티티 내부에 **중첩 enum**으로 선언, `@Enumerated(EnumType.STRING)` 매핑
- 예: `Member.Role { SYSTEM, ADMIN, USER }` (3단 권한 계층),
  `Member.Status { ACTIVE, WITHDRAWN }` (soft-delete: 탈퇴해도 row 유지)

### 시간 필드
- `createdAt`(등록), `updatedAt`(수정) — `LocalDateTime`
- `@PrePersist`에서 생성 시각, `@PreUpdate`에서 수정 시각 세팅
- `createdAt`은 `updatable = false`

### 공통 어노테이션 패턴
```java
@Entity
@Table(name = "xxx")
@Getter
@NoArgsConstructor(access = AccessLevel.PROTECTED)
public class Xxx {
    // ...
    @Builder
    public Xxx(...) { ... }   // 생성은 빌더로
}
```
- 연관관계는 기본 `@ManyToOne(fetch = FetchType.LAZY)`
- 수정은 setter 대신 **의도가 드러나는 메서드**로: `update(...)`, `changeRole(...)`, `withdraw()`, `changeImageUrl(...)`
- 개인정보(전화번호·주소·CI 등)는 `@Convert(converter = AesAttributeConverter.class)`로 AES-256 암호화 저장

---

## 3. 리포지토리 (Repository)

| 대상 | 규칙 | 예시 |
|---|---|---|
| 인터페이스명 | `엔티티명 + Repository` | `ProductRepository`, `BannerRepository` |
| 상속 | `JpaRepository<엔티티, PK타입>` | `JpaRepository<Product, Long>`, `JpaRepository<Member, String>` |

- 단순 조회는 **Spring Data 파생 쿼리** 메서드명 규칙을 따른다
  - `findByXxx`, `existsByXxx`, `countByXxx`, `findByXxxOrderByYyyDesc`
  - 예: `findByUserId`, `existsByUserId`, `findByActiveTrueOrderBySortOrderAsc`,
    `findByMarqueeTrueOrderByCreatedAtDesc`, `findAllByOrderByPinnedDescCreatedAtDesc`
- 복잡한 쿼리는 `@Query`(JPQL) 사용
  - 예: `findPopularProducts` (LEFT JOIN + GROUP BY + ORDER BY)

---

## 4. 서비스 (Service)

| 대상 | 규칙 | 예시 |
|---|---|---|
| 클래스명 | `도메인명 + Service` | `ProductService`, `BannerService` |
| 의존성 주입 | `final` 필드 + `@RequiredArgsConstructor` (생성자 주입) | |

```java
@Service
@RequiredArgsConstructor
@Transactional(readOnly = true)   // 클래스 기본은 읽기 전용
public class ProductService {
    @Transactional                // 쓰기 메서드만 재정의
    public Long createProduct(...) { ... }
}
```

- 메서드명은 동사로 시작: `getProducts`, `getProduct`, `create`, `update`, `delete`
- 권한 단계를 클래스/서비스 이름에 박지 않는다 (조회·관리 모두 한 서비스에서 담당)

---

## 5. 컨트롤러 (Controller) · URL

| 대상 | 규칙 | 예시 |
|---|---|---|
| 클래스명 | `도메인명 + Controller` | `ProductController`, `BannerController` |
| URL 리소스 | **소문자 · 복수형 명사** | `/products`, `/orders`, `/admin/products` |
| 다단어 **액션** 세그먼트 | **camelCase** | `/auth/checkId`, `/auth/logoutAfterWithdraw`, `/mypage/changePassword` |
| 인증 | `/auth/...` | `/auth/login`, `/auth/signup`, `/auth/logout` |
| 관리자 | `/admin/...` (ROLE_ADMIN 전용) | `/admin/products`, `/admin/banners` |

> **권한별로 클래스를 쪼개지 않는다.** 한 도메인은 컨트롤러·서비스 하나로 두고,
> 관리자 기능은 클래스 이름(`Admin*`)이 아니라 `/admin/**` URL(SecurityConfig)로만 구분한다.
> 예) 공지 조회·관리는 `NoticeController` 한 곳, 상품 조회·관리는 `ProductController` 한 곳.

### CRUD URL · 핸들러 메서드 규칙

> **원칙: URL의 마지막 동사 = 메서드명으로 통일한다.**
> 폼(GET)과 처리(POST)는 **같은 URL**을 쓰고, GET 폼 메서드만 `Form` 접미사를 붙인다.

| 기능 | HTTP · URL | 메서드명 |
|---|---|---|
| 목록 | `GET /products` | `list` |
| 상세 | `GET /products/{id}` | `detail` |
| 등록 폼 | `GET /products/register` | `registerForm` |
| 등록 처리 | `POST /products/register` | `register` |
| 수정 폼 | `GET /products/{id}/edit` | `editForm` |
| 수정 처리 | `POST /products/{id}/edit` | `edit` |
| 삭제 처리 | `POST /products/{id}/delete` | `delete` |

- 조회는 GET, 상태 변경(등록·수정·삭제)은 **POST** (삭제도 GET 아님)
- 리스트 + 모달로 처리하는 화면(공지·배너·문의)은 별도 폼 GET 없이
  `POST .../register`, `POST .../{id}/edit`, `POST .../{id}/delete` 만 둔다
- 처리 후에는 `RedirectAttributes` 로 `successMsg`/`errorMsg` 플래시 → `redirect:`

---

## 6. 뷰 (JSP)

| 대상 | 규칙 | 예시 |
|---|---|---|
| 경로 | `/WEB-INF/views/{리소스}/{화면}.jsp` | `products/list.jsp`, `admin/banners/list.jsp` |
| 파일명 | 소문자 | `list`, `detail`, `form`, `upload`, `login` |
| 공통 조각 | `layout/` | `layout/header.jsp`, `layout/meta.jsp` |

### `layout/` 공통 조각
| 파일 | 역할 |
|---|---|
| `meta.jsp` | `<head>` 안 리소스(viewport·웹폰트·Bootstrap·아이콘·theme.css). `<title>` 아래에서 include |
| `header.jsp` | 공개 페이지 상단 네비게이션 바(로고·메뉴) |
| `admin-topbar.jsp` / `admin-sidebar.jsp` | 관리자 화면 상단바·사이드바 |
| `flash-toast.jsp` | `successMsg`/`errorMsg` 토스트 |

- 등록·수정 화면은 가능하면 **하나의 `form.jsp`**로 공용화(상품). 모델에 수정 대상 객체 유무로 분기
- 단순 CRUD는 **리스트 1장 + 모달(등록/상세·수정/삭제)** 패턴(공지·배너·문의)
- taglib 접두사: `c`(JSTL core), `fmt`(포맷), `sec`(Spring Security)
- 모든 POST 폼에는 CSRF 토큰 포함:
  ```jsp
  <input type="hidden" name="${_csrf.parameterName}" value="${_csrf.token}"/>
  ```
- 로컬 업로드 이미지(`/uploads/...`)는 `${pageContext.request.contextPath}` 를 앞에 붙이고,
  외부 URL(Supabase 등 `https://...`)은 그대로 사용한다

---

## 7. DB 스키마 (TiDB / MySQL 호환)

스키마는 **Hibernate `spring.jpa.hibernate.ddl-auto=update`** 로 엔티티에서 자동 생성·반영한다.
(별도 `schema.sql` 원본은 두지 않는다. 표 이름·컬럼은 아래 엔티티 매핑 규칙으로 결정된다.)

| 대상 | 규칙 | 예시 |
|---|---|---|
| 테이블명 | snake_case, **단수** | `member`, `product`, `order_item` |
| 예외 | SQL 예약어는 회피 | `orders` (`order`가 예약어) |
| PK 컬럼 | `테이블명_id` | `product_id`, `member_id`, `banner_id` |
| FK 컬럼 | `참조테이블_id` | `category_id`, `order_id` |
| boolean 컬럼 | `is_xxx` | `is_active`, `is_pinned` |
| 일반 컬럼 | snake_case | `created_at`, `sort_order` |

- 회원 PK/참조 FK 는 `CHAR(36)`(UUID), 그 외 PK/FK 는 `BIGINT`
- 개인정보 암호화 컬럼은 길이를 넉넉히(`length = 500`) — Base64 암호문 저장용
- 수동으로 DDL/인덱스를 작성할 경우의 권장 접두사: 인덱스 `idx_테이블_컬럼`,
  FK 제약 `fk_테이블_참조`, UNIQUE `uk_테이블_컬럼` (현재는 ddl-auto 가 자동 생성)

---

## 8. CSS

| 파일 | 범위 |
|---|---|
| `static/css/theme.css` | **전역 공통** 테마(색·네비·버튼·카테고리·마퀴 등). `meta.jsp` 로 모든 페이지 로드 |
| `static/css/main.css` | 메인 페이지 전용(히어로·캐러셀·상품 카드 등) |
| `static/css/admin.css` | 관리자 화면 전용 |

- 클래스명: **kebab-case** — `product-card`, `section-title`, `rank-badge`, `hero-search`,
  `cat-grid`, `cat-item`, `notice-marquee-bar`, `back-link`
- 전역에서 재사용되는 스타일은 `theme.css`, 특정 페이지 전용만 해당 페이지 css 에 둔다

---

## 9. 요약 체크리스트

새 도메인을 추가할 때:

- [ ] 패키지: `com.kevolution.{도메인}.{controller|service|repository|entity}` (도메인 우선)
- [ ] 엔티티: PascalCase 단수, 테이블 snake_case 단수, PK `Long xxxId`(회원 참조는 `CHAR(36)`)
- [ ] boolean은 `is_xxx` 컬럼 + `is` 뺀 필드명, 수정은 의도가 드러나는 메서드로
- [ ] Repository: `XxxRepository extends JpaRepository<Xxx, IdType>`
- [ ] Service: `@Transactional(readOnly=true)` 기본 + 쓰기 메서드 `@Transactional`
- [ ] Controller: 소문자 복수 URL(다단어 액션은 camelCase), CRUD는 list/detail/registerForm/register/editForm/edit/delete (URL 동사 = 메서드명)
- [ ] JSP: `/WEB-INF/views/{리소스}/{화면}.jsp`, 공통 조각은 `layout/`, POST 폼엔 CSRF 토큰
- [ ] CSS: 전역은 `theme.css` · 메인은 `main.css` · 관리자는 `admin.css`, 클래스 kebab-case
- [ ] 스키마는 ddl-auto=update 로 자동 반영 (수동 DDL 작성 시 `idx_`/`fk_`/`uk_` 접두사)
